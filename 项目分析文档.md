# PromptHub 项目分析文档

## 一、项目主要功能

**PromptHub** 是一款本地优先、跨平台的一站式 AI 工具箱，主要功能包括：

### 1. Prompt 管理
- 创建、编辑、删除 Prompt，支持文件夹和标签分类
- 自动保存历史版本，支持查看、对比和回滚
- 模板变量 `{{variable}}` 语法，支持动态填充
- 快速收藏常用 Prompt，一键访问
- 全文搜索、附件与多媒体预览

### 2. Skill 分发与管理
- **技能商店**：内置 20+ 精选技能（来自 Anthropic、OpenAI 等）
- **多平台安装**：一键安装到 Claude Code、Cursor、Windsurf、Codex、Kiro、Gemini CLI、Qoder 等 15+ 平台
- **本地扫描**：自动发现本地已有 SKILL.md，预览选择后导入
- **软链接/复制模式**：支持 Symlink 同步编辑或独立复制
- **AI 翻译与润色**：沉浸式/全文翻译技能内容

### 3. 项目与 Agent 资产管理
- 扫描项目中的 `.claude/skills`、`.agents/skills`、`skills`、`.gemini` 等常见目录
- 统一管理个人库、本地仓库和项目里的 AI 资产

### 4. AI 测试与生成
- 内置 AI 测试，支持国内外主流服务商
- 覆盖各类主流大语言模型、开源及闭源模型
- 同一 Prompt 多模型并行测试对比
- AI 生成技能内容、智能润色

### 5. 数据与同步
- 所有数据存储在本地，隐私安全有保障
- 全量备份与恢复（`.phub.gz` 压缩格式）
- WebDAV 云同步（坚果云、Nextcloud 等）
- 支持自部署 PromptHub Web 作为桌面版备份源/恢复源
- 支持启动同步 + 定时同步

### 6. 界面与安全
- 多视图模式：卡片、画廊、列表
- 深色/浅色/跟随系统，多种主题色
- 支持自定义背景图片
- 7 种语言支持（中/英/日/法/德/西/繁中）
- Markdown 渲染与代码高亮
- 跨平台：macOS / Windows / Linux
- 主密码保护、私密文件夹加密存储

---

## 二、技术栈分析

### 编程语言
- **TypeScript 5** - 主要开发语言

### 前端技术栈
| 类别 | 技术 |
|------|------|
| UI 框架 | React 18 |
| 构建工具 | Vite 6 |
| 样式 | Tailwind CSS 3 |
| 状态管理 | Zustand 5 |
| 图标 | Lucide React |
| 动画 | Framer Motion |
| 国际化 | i18next 24 / react-i18next 15 |
| Markdown 渲染 | react-markdown + rehype-highlight |
| 拖拽 | @dnd-kit/core |

### 后端/桌面技术栈
| 类别 | 技术 |
|------|------|
| 桌面 Runtime | Electron 33 |
| 数据库 | SQLite (better-sqlite3 / node-sqlite3-wasm) |
| Web 框架 | Hono (自部署 Web) |
| 加密 | AES-256-GCM |
| 压缩 | fflate |
| 云同步 | WebDAV / S3 |

### 测试与开发工具
| 类别 | 技术 |
|------|------|
| 单元测试 | Vitest 2 |
| E2E 测试 | Playwright 1.57+ |
| 代码规范 | ESLint 9 + Prettier 3 |
| 包管理 | pnpm 9.15.0 |

---

## 三、顶层目录结构及作用

```
PromptHub/
├── apps/                      # 应用程序目录（Monorepo 应用层）
│   ├── cli/                   # 命令行工具
│   │   ├── bin/               # CLI 可执行文件入口
│   │   ├── src/               # CLI 源码
│   │   └── tests/             # CLI 测试
│   ├── desktop/               # Electron 桌面端应用
│   │   ├── resources/         # 应用资源（图标、安装脚本）
│   │   ├── scripts/           # 构建/发布脚本
│   │   ├── src/
│   │   │   ├── main/          # Electron 主进程
│   │   │   │   ├── database/  # SQLite 数据库层
│   │   │   │   ├── ipc/       # IPC 通信处理器
│   │   │   │   ├── services/  # 业务逻辑服务
│   │   │   │   └── settings/  # 设置读取器
│   │   │   ├── preload/       # Electron 预加载脚本
│   │   │   ├── renderer/      # React 前端渲染进程
│   │   │   │   ├── components/# UI 组件
│   │   │   │   ├── hooks/     # React Hooks
│   │   │   │   ├── i18n/      # 国际化配置
│   │   │   │   ├── services/  # 前端服务
│   │   │   │   ├── stores/    # Zustand 状态管理
│   │   │   │   └── styles/    # 全局样式
│   │   │   └── types/         # TypeScript 类型定义
│   │   └── tests/             # 桌面端测试
│   │       ├── e2e/           # Playwright E2E 测试
│   │       ├── fixtures/      # 测试夹具
│   │       └── unit/          # Vitest 单元测试
│   └── web/                   # 自部署 Web 版
│       ├── src/
│       │   ├── client/        # Web 前端客户端
│       │   ├── middleware/    # Hono 中间件
│       │   ├── routes/        # API 路由
│       │   └── services/      # 后端服务
│       └── scripts/           # Web 部署脚本
│
├── packages/                  # 共享包（Monorepo 包层）
│   ├── core/                  # 核心共享逻辑
│   │   └── src/
│   │       ├── cli/           # CLI 共享逻辑
│   │       ├── skills/        # Skill 安装流程
│   │       └── *.ts           # 核心模块
│   ├── db/                    # 数据库层共享包
│   │   └── src/
│   │       ├── adapter.ts     # 数据库适配器
│   │       ├── schema.ts      # 数据库 Schema
│   │       └── *.ts           # CRUD 类
│   └── shared/                # 共享类型与常量
│       ├── constants/         # IPC 通道、平台常量等
│       └── types/             # TypeScript 类型定义
│
├── docs/                      # 对外文档
│   ├── imgs/                  # 文档图片资源
│   ├── README.*.md            # 多语言 README
│   ├── contributing.md        # 贡献指南
│   └── web-self-hosted.md     # 自部署 Web 文档
│
├── .agents/                   # AI Agent 配置
│   ├── rules/                 # Agent 规则
│   ├── skills/                # Agent Skills
│   └── workflows/             # Agent 工作流
│
├── .github/                   # GitHub 配置
│   ├── ISSUE_TEMPLATE/        # Issue 模板
│   ├── scripts/               # GitHub 脚本
│   └── workflows/             # GitHub Actions
│
├── spec/                      # 内部 SSD/Spec 系统
│   ├── domains/               # 稳定领域文档
│   ├── architecture/          # 架构文档
│   ├── logic/                 # 业务逻辑文档
│   ├── changes/               # 变更记录
│   └── issues/                # 活跃问题
│
├── package.json               # 根 package.json (Monorepo 配置)
├── README.md                  # 项目主 README
├── CHANGELOG.md               # 更新日志
├── CONTRIBUTING.md            # 贡献指南
└── LICENSE                    # AGPL-3.0 许可证
```

### 目录作用详解

| 目录 | 作用 |
|------|------|
| `apps/cli` | 独立的命令行工具，支持脚本化管理、批量导入导出 |
| `apps/desktop` | Electron 桌面应用，包含主进程、渲染进程、预加载脚本 |
| `apps/web` | 自部署 Web 版，基于 Hono + React，支持 Docker 部署 |
| `packages/core` | 共享核心逻辑，被 CLI、Desktop、Web 共用 |
| `packages/db` | 共享数据库层，封装 SQLite 操作 |
| `packages/shared` | 共享类型定义和常量，跨包复用 |
| `docs/` | 用户文档、截图、多语言 README |
| `spec/` | 内部规范文档系统（SSD），记录架构、领域知识 |
| `.agents/` | AI Agent 相关配置和 Skills |

---

## 四、架构特点

### 1. Monorepo 架构
- 使用 pnpm workspaces 管理
- 清晰的 `apps/` 和 `packages/` 分层
- 共享包通过 `workspace:*` 协议引用

### 2. Electron 进程模型
- **Main Process**: 处理原生 OS 交互、文件系统、数据库、安全
- **Renderer Process**: React SPA 负责 UI/UX
- **Preload**: 通过 `contextBridge` 安全暴露 API

### 3. 数据层设计
- **本地优先**: SQLite 数据库存储所有数据
- **FTS5 全文搜索**: 支持 Prompt 全文检索
- **版本控制**: Prompt 和 Skill 支持历史版本管理
- **加密存储**: 私密文件夹内容加密

### 4. 多平台支持
- 桌面端: macOS / Windows / Linux
- Web 端: 自部署浏览器访问
- CLI: 命令行脚本化管理

---

## 五、版本信息

- **当前版本**: v0.5.6-beta.2
- **许可证**: AGPL-3.0
- **Node 要求**: >= 22.0.0
- **包管理器**: pnpm 9.15.0

---

*文档生成时间: 2026-05-16*
